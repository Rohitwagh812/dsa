// Creating an array 

let arr = [1, 2, 3, 4, 5];
console.log(arr); // Output: [1, 2, 3, 4, 10]

// Accessing elements
console.log( 'Accessing elements'+' => ' + arr[0]); // Output: 1

// Modifying elements
arr[1] = 10;

// Iterating through an array
for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}